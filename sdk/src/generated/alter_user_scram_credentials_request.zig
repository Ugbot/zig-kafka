//! Auto-generated Kafka protocol message
//! Message: AlterUserScramCredentialsRequest
//! API Key: 51
//! Type: request
//! Valid Versions: 0
//! Flexible Versions: 0+
//!
//! DO NOT EDIT - Generated from protocol JSON

const std = @import("std");
const types = @import("../protocol/types.zig");

/// Nested struct: ScramCredentialUpsertion
pub const ScramCredentialUpsertion = struct {
    const Self = @This();

    /// The user name.
    /// Versions: 0+
    name: []const u8 = "",
    /// The SCRAM mechanism.
    /// Versions: 0+
    mechanism: i8 = 0,
    /// The number of iterations.
    /// Versions: 0+
    iterations: i32 = 0,
    /// A random salt generated by the client.
    /// Versions: 0+
    salt: []const u8 = &[_]u8{},
    /// The salted password.
    /// Versions: 0+
    salted_password: []const u8 = &[_]u8{},

    /// Tagged fields for forward compatibility
    _tagged_fields: ?[]types.TaggedField = null,

    pub fn encode(self: *const Self, writer: anytype, version: i16) !void {
        const is_flexible = isFlexibleVersion(version);
        _ = &is_flexible;

        // Field: Name
        if (version >= 0 and version <= 32767) {
            if (is_flexible) {
                try types.encodeCompactString(writer, self.name);
            } else {
                try types.encodeString(writer, self.name);
            }
        }

        // Field: Mechanism
        if (version >= 0 and version <= 32767) {
            try types.encodeInt8(writer, self.mechanism);
        }

        // Field: Iterations
        if (version >= 0 and version <= 32767) {
            try types.encodeInt32(writer, self.iterations);
        }

        // Field: Salt
        if (version >= 0 and version <= 32767) {
            if (is_flexible) {
                try types.encodeCompactBytes(writer, self.salt);
            } else {
                try types.encodeBytes(writer, self.salt);
            }
        }

        // Field: SaltedPassword
        if (version >= 0 and version <= 32767) {
            if (is_flexible) {
                try types.encodeCompactBytes(writer, self.salted_password);
            } else {
                try types.encodeBytes(writer, self.salted_password);
            }
        }


        if (is_flexible) {
            if (self._tagged_fields) |fields| {
                try types.encodeTaggedFields(writer, fields);
            } else {
                try types.encodeUnsignedVarInt(writer, 0);
            }
        }
    }

    pub fn computeSize(self: *const Self, version: i16) !usize {
        const is_flexible = isFlexibleVersion(version);
        _ = &is_flexible;
        var total_size: usize = 0;

        // Field: Name
        if (version >= 0 and version <= 32767) {
            total_size += if (is_flexible) types.computeSizeCompactString(self.name) else types.computeSizeString(self.name);
        }

        // Field: Mechanism
        if (version >= 0 and version <= 32767) {
            total_size += types.computeSizeInt8(self.mechanism);
        }

        // Field: Iterations
        if (version >= 0 and version <= 32767) {
            total_size += types.computeSizeInt32(self.iterations);
        }

        // Field: Salt
        if (version >= 0 and version <= 32767) {
            total_size += if (is_flexible) types.computeSizeCompactBytes(self.salt) else types.computeSizeBytes(self.salt);
        }

        // Field: SaltedPassword
        if (version >= 0 and version <= 32767) {
            total_size += if (is_flexible) types.computeSizeCompactBytes(self.salted_password) else types.computeSizeBytes(self.salted_password);
        }


        if (is_flexible) {
            if (self._tagged_fields) |fields| {
                total_size += types.computeSizeTaggedFields(fields);
            } else {
                total_size += 1; // Empty tagged fields marker
            }
        }
        return total_size;
    }

    pub fn decode(reader: anytype, version: i16, allocator: std.mem.Allocator) !Self {
        const is_flexible = isFlexibleVersion(version);
        _ = &is_flexible;
        _ = &allocator;
        var self: Self = .{};
        // Field: Name
        if (version >= 0 and version <= 32767) {
            self.name = if (is_flexible)
                try types.decodeCompactString(reader, allocator) orelse ""
            else
                try types.decodeString(reader, allocator) orelse "";
        }

        // Field: Mechanism
        if (version >= 0 and version <= 32767) {
            self.mechanism = try types.decodeInt8(reader);
        }

        // Field: Iterations
        if (version >= 0 and version <= 32767) {
            self.iterations = try types.decodeInt32(reader);
        }

        // Field: Salt
        if (version >= 0 and version <= 32767) {
            self.salt = if (is_flexible)
                try types.decodeCompactBytes(reader, allocator) orelse ""
            else
                try types.decodeBytes(reader, allocator) orelse "";
        }

        // Field: SaltedPassword
        if (version >= 0 and version <= 32767) {
            self.salted_password = if (is_flexible)
                try types.decodeCompactBytes(reader, allocator) orelse ""
            else
                try types.decodeBytes(reader, allocator) orelse "";
        }


        if (is_flexible) {
            const tagged_fields_data = try types.decodeTaggedFields(reader, allocator);
            self._tagged_fields = tagged_fields_data;
        }
        return self;
    }

    fn isFlexibleVersion(version: i16) bool {
        const range = types.VersionRange.parse("0+") catch return false;
        return range.contains(version);
    }
};

/// Nested struct: ScramCredentialDeletion
pub const ScramCredentialDeletion = struct {
    const Self = @This();

    /// The user name.
    /// Versions: 0+
    name: []const u8 = "",
    /// The SCRAM mechanism.
    /// Versions: 0+
    mechanism: i8 = 0,

    /// Tagged fields for forward compatibility
    _tagged_fields: ?[]types.TaggedField = null,

    pub fn encode(self: *const Self, writer: anytype, version: i16) !void {
        const is_flexible = isFlexibleVersion(version);
        _ = &is_flexible;

        // Field: Name
        if (version >= 0 and version <= 32767) {
            if (is_flexible) {
                try types.encodeCompactString(writer, self.name);
            } else {
                try types.encodeString(writer, self.name);
            }
        }

        // Field: Mechanism
        if (version >= 0 and version <= 32767) {
            try types.encodeInt8(writer, self.mechanism);
        }


        if (is_flexible) {
            if (self._tagged_fields) |fields| {
                try types.encodeTaggedFields(writer, fields);
            } else {
                try types.encodeUnsignedVarInt(writer, 0);
            }
        }
    }

    pub fn computeSize(self: *const Self, version: i16) !usize {
        const is_flexible = isFlexibleVersion(version);
        _ = &is_flexible;
        var total_size: usize = 0;

        // Field: Name
        if (version >= 0 and version <= 32767) {
            total_size += if (is_flexible) types.computeSizeCompactString(self.name) else types.computeSizeString(self.name);
        }

        // Field: Mechanism
        if (version >= 0 and version <= 32767) {
            total_size += types.computeSizeInt8(self.mechanism);
        }


        if (is_flexible) {
            if (self._tagged_fields) |fields| {
                total_size += types.computeSizeTaggedFields(fields);
            } else {
                total_size += 1; // Empty tagged fields marker
            }
        }
        return total_size;
    }

    pub fn decode(reader: anytype, version: i16, allocator: std.mem.Allocator) !Self {
        const is_flexible = isFlexibleVersion(version);
        _ = &is_flexible;
        _ = &allocator;
        var self: Self = .{};
        // Field: Name
        if (version >= 0 and version <= 32767) {
            self.name = if (is_flexible)
                try types.decodeCompactString(reader, allocator) orelse ""
            else
                try types.decodeString(reader, allocator) orelse "";
        }

        // Field: Mechanism
        if (version >= 0 and version <= 32767) {
            self.mechanism = try types.decodeInt8(reader);
        }


        if (is_flexible) {
            const tagged_fields_data = try types.decodeTaggedFields(reader, allocator);
            self._tagged_fields = tagged_fields_data;
        }
        return self;
    }

    fn isFlexibleVersion(version: i16) bool {
        const range = types.VersionRange.parse("0+") catch return false;
        return range.contains(version);
    }
};

/// AlterUserScramCredentialsRequest
pub const AlterUserScramCredentialsRequest = struct {
    const Self = @This();

    /// The SCRAM credentials to remove.
    deletions: ?[]ScramCredentialDeletion = null,
    /// The SCRAM credentials to update/insert.
    upsertions: ?[]ScramCredentialUpsertion = null,

    /// Tagged fields for forward compatibility
    _tagged_fields: ?[]types.TaggedField = null,

    // ============================================================================
    // METHODS
    // ============================================================================

    /// Version range for this message
    pub const VERSIONS = types.VersionRange{ .min = 0, .max = 0 };

    /// API Key for this message
    pub fn apiKey() i16 {
        return 51;
    }

    /// Create a default instance of AlterUserScramCredentialsRequest
    pub fn default() Self {
        return .{
            .deletions = null,
            .upsertions = null,
            ._tagged_fields = null,
        };
    }

    /// Sets `deletions` to the passed value.
    /// The SCRAM credentials to remove.
    pub fn withDeletions(self: Self, value: ?[]ScramCredentialDeletion) Self {
        var result = self;
        result.deletions = value;
        return result;
    }

    /// Sets `upsertions` to the passed value.
    /// The SCRAM credentials to update/insert.
    pub fn withUpsertions(self: Self, value: ?[]ScramCredentialUpsertion) Self {
        var result = self;
        result.upsertions = value;
        return result;
    }

    /// Encode AlterUserScramCredentialsRequest
    pub fn encode(self: *const Self, writer: anytype, version: i16) !void {
        if (!isValidVersion(version)) {
            return types.Error.UnsupportedVersion;
        }
        const is_flexible = isFlexibleVersion(version);
        _ = &is_flexible;

        // Field: Deletions
        if (version >= 0 and version <= 32767) {
            if (is_flexible) {
                try types.encodeCompactArrayLenNonNull(writer, self.deletions);
                if (self.deletions) |arr| {
                    for (arr) |*item| {
                        try ScramCredentialDeletion.encode(item, writer, version);
                    }
                }
            } else {
                try types.encodeArrayLenNonNull(writer, self.deletions);
                if (self.deletions) |arr| {
                    for (arr) |*item| {
                        try ScramCredentialDeletion.encode(item, writer, version);
                    }
                }
            }
        }

        // Field: Upsertions
        if (version >= 0 and version <= 32767) {
            if (is_flexible) {
                try types.encodeCompactArrayLenNonNull(writer, self.upsertions);
                if (self.upsertions) |arr| {
                    for (arr) |*item| {
                        try ScramCredentialUpsertion.encode(item, writer, version);
                    }
                }
            } else {
                try types.encodeArrayLenNonNull(writer, self.upsertions);
                if (self.upsertions) |arr| {
                    for (arr) |*item| {
                        try ScramCredentialUpsertion.encode(item, writer, version);
                    }
                }
            }
        }


        if (is_flexible) {
            var num_tagged_fields: u32 = 0;

            // Count unknown tagged fields
            if (self._tagged_fields) |fields| {
                num_tagged_fields += @intCast(fields.len);
            }

            try types.encodeUnsignedVarInt(writer, num_tagged_fields);

            // Encode unknown tagged fields
            if (self._tagged_fields) |fields| {
                try types.encodeTaggedFields(writer, fields);
            }
        }
    }

    /// Compute the size of AlterUserScramCredentialsRequest for the given version
    pub fn computeSize(self: *const Self, version: i16) !usize {
        if (!isValidVersion(version)) {
            return types.Error.UnsupportedVersion;
        }
        const is_flexible = isFlexibleVersion(version);
        _ = &is_flexible;

        var total_size: usize = 0;

        // Field: Deletions
        if (version >= 0 and version <= 32767) {
            if (self.deletions) |arr| {
                const len: u32 = @intCast(arr.len + 1);
                total_size += if (is_flexible) types.computeSizeUnsignedVarInt(len) else 4;
                for (arr) |*item| {
                    total_size += try ScramCredentialDeletion.computeSize(item, version);
                }
            } else {
                total_size += if (is_flexible) types.computeSizeUnsignedVarInt(1) else 4;
            }
        }

        // Field: Upsertions
        if (version >= 0 and version <= 32767) {
            if (self.upsertions) |arr| {
                const len: u32 = @intCast(arr.len + 1);
                total_size += if (is_flexible) types.computeSizeUnsignedVarInt(len) else 4;
                for (arr) |*item| {
                    total_size += try ScramCredentialUpsertion.computeSize(item, version);
                }
            } else {
                total_size += if (is_flexible) types.computeSizeUnsignedVarInt(1) else 4;
            }
        }

        if (is_flexible) {
            var num_tagged_fields: u32 = 0;

            if (self._tagged_fields) |fields| {
                num_tagged_fields += @intCast(fields.len);
            }

            total_size += types.computeSizeUnsignedVarInt(num_tagged_fields);

            if (self._tagged_fields) |fields| {
                total_size += types.computeSizeTaggedFields(fields);
            }
        }

        return total_size;
    }

    /// Decode AlterUserScramCredentialsRequest
    pub fn decode(reader: anytype, version: i16, allocator: std.mem.Allocator) !Self {
        if (!isValidVersion(version)) {
            return types.Error.UnsupportedVersion;
        }
        const is_flexible = isFlexibleVersion(version);
        var self: Self = .{};

        // Field: Deletions
        if (version >= 0 and version <= 32767) {
            const array_len = if (is_flexible) try types.decodeCompactArrayLen(reader) else try types.decodeArrayLen(reader);
            const array = try allocator.alloc(ScramCredentialDeletion, array_len);
            for (array) |*item| {
                item.* = try ScramCredentialDeletion.decode(reader, version, allocator);
            }
            self.deletions = array;
        }

        // Field: Upsertions
        if (version >= 0 and version <= 32767) {
            const array_len = if (is_flexible) try types.decodeCompactArrayLen(reader) else try types.decodeArrayLen(reader);
            const array = try allocator.alloc(ScramCredentialUpsertion, array_len);
            for (array) |*item| {
                item.* = try ScramCredentialUpsertion.decode(reader, version, allocator);
            }
            self.upsertions = array;
        }


        if (is_flexible) {
            const num_tagged_fields = try types.decodeUnsignedVarInt(reader);
            var unknown_tagged_fields = std.array_list.Managed(types.TaggedField).init(allocator);

            var i: u32 = 0;
            while (i < num_tagged_fields) : (i += 1) {
                const tag = try types.decodeUnsignedVarInt(reader);
                const size = try types.decodeUnsignedVarInt(reader);
                const field_data = try allocator.alloc(u8, size);
                _ = try reader.readAll(field_data);
                try unknown_tagged_fields.append(.{ .tag = tag, .data = field_data });
            }

            if (unknown_tagged_fields.items.len > 0) {
                self._tagged_fields = try unknown_tagged_fields.toOwnedSlice();
            }
        }

        return self;
    }

    /// Check if version is valid
    pub fn isValidVersion(version: i16) bool {
        const range = types.VersionRange.parse("0") catch return false;
        return range.contains(version);
    }
    /// Check if version uses flexible encoding
    pub fn isFlexibleVersion(version: i16) bool {
        const range = types.VersionRange.parse("0+") catch return false;
        return range.contains(version);
    }
    /// Get the header version for this API version
    /// Returns 2 for flexible versions, 1 for classic versions
    pub fn headerVersion(api_version: i16) i16 {
        if (isFlexibleVersion(api_version)) return 2;
        return 1;
    }

};
